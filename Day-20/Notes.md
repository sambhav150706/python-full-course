📘 NOTES (Day-20)
1️⃣ What is a Python Library

A library is pre-written code that helps you perform tasks easily.

Examples:

requests → HTTP requests

numpy → math & arrays

pandas → data analysis

2️⃣ Built-in vs External Libraries

Built-in:

math

random

datetime

External:

requests

flask

django

3️⃣ Installing a Library
pip install requests


Check installed libraries:

pip list

4️⃣ Virtual Environment (Important)

A virtual environment keeps project dependencies separate.

Create venv:

python -m venv venv


Activate:

Windows:

venv\Scripts\activate


Mac/Linux:

source venv/bin/activate


Deactivate:

deactivate
